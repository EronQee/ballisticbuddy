# i18n A 期进度

## Date/Commit
- 2026-09-02 · commit `0f27edb` · branch `feat/i18n-next-intl-a` (pushed to origin)

## Scope
- 落地 next-intl 4.14.2 作为本项目 i18n 实现（A 期：路由 + UI 字典基础设施，不碰 DB schema）
- 6 个 locale：en(默认) / pt / ar(RTL) / es / fr / ru，单一事实来源 `src/i18n/localization.ts`
- 前台路由整体下移 `(frontend)/[locale]/`，`next/*`、`(sitemaps)`、`admin` 留在外面
- `localePrefix: 'as-needed'`：英文在根路径 `/`，其余带前缀（与 SEO 文档一致）

## Files
- 新增：`src/i18n/localization.ts`、`src/i18n/routing.ts`、`src/i18n/request.ts`、`src/proxy.ts`、`src/global.d.ts`、`messages/{en,pt,ar,es,fr,ru}.json`
- 修改：`next.config.ts`（挂 `createNextIntlPlugin`）、`package.json`（+next-intl）
- 移动+改：`src/app/(frontend)/*` → `src/app/(frontend)/[locale]/*`（layout 重写为 locale-aware）
- 文档：`documention/harness/MEMORY.md`（新增 i18n 坑一节）

## Validation
- `pnpm build` 全绿：6 locale 全部 SSG 预渲染，admin/api/sitemap/next/* 完好，Proxy 生效
- 冒烟（next start :3210）：`/`→200(英文根)、`/en`/`/pt`→200、`/ar`→200 `dir=rtl`、`/admin/login`→200
- `pnpm lint` 0 错误；`node scripts/check-guardrails.mjs` PASS

## Deployment Notes
- **payload.config.ts 未挂 `localization`**（开启即需 `*_locales` 表 → 生产 migration，B 期再上）
- 本地 dev 需重启（A 期开发时杀过旧 next dev 实例）
- Vercel 部署无新增 env；但需留意 proxy.ts（Next 16 middleware 改名）在 Vercel 上的 matcher 行为

## B 期待办
- Payload Localization + migration（staging `pnpm payload migrate:create` + `migrate`）
- sitemap hreflang / 多语言 URL 枚举
- Header/Footer 导航换 `createNavigation` 的 `Link`
- N8n 自动翻译管线（Payload REST API `?locale=all` 读 → 翻译 → PATCH 回写）
