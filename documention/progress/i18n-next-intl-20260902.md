# i18n A 期进度

## Date/Commit
- 2026-09-02 · commit `0f27edb` · branch `feat/i18n-next-intl-a` (pushed to origin)

## Scope
- 落地 next-intl 4.14.2 作为本项目 i18n 实现（A 期：路由 + UI 字典基础设施，不碰 DB schema）
- 6 个 locale：en(默认) / pt / ar(RTL) / es / fr / ru，单一事实来源 `src/i18n/localization.ts`
- 前台路由整体下移 `(frontend)/[locale]/`，`next/*`、`(sitemaps)`、`admin` 留在外面
- `localePrefix: 'as-needed'`：英文在根路径 `/`，其余带前缀（与 SEO 文档一致）

## Files
- 新增：`src/i18n/localization.ts`、`src/i18n/routing.ts`、`src/i18n/request.ts`、`src/proxy.ts`、`src/global.d.ts`、`messages/{en,pt,ar,es,fr,ru}.json`
- 修改：`next.config.ts`（挂 `createNextIntlPlugin`）、`package.json`（+next-intl）
- 移动+改：`src/app/(frontend)/*` → `src/app/(frontend)/[locale]/*`（layout 重写为 locale-aware）
- 文档：`documention/harness/MEMORY.md`（新增 i18n 坑一节）

## Validation
- `pnpm build` 全绿：6 locale 全部 SSG 预渲染，admin/api/sitemap/next/* 完好，Proxy 生效
- 冒烟（next start :3210）：`/`→200(英文根)、`/en`/`/pt`→200、`/ar`→200 `dir=rtl`、`/admin/login`→200
- `pnpm lint` 0 错误；`node scripts/check-guardrails.mjs` PASS

## Deployment Notes
- **payload.config.ts 未挂 `localization`**（开启即需 `*_locales` 表 → 生产 migration，B 期再上）
- 本地 dev 需重启（A 期开发时杀过旧 next dev 实例）
- Vercel 部署无新增 env；但需留意 proxy.ts（Next 16 middleware 改名）在 Vercel 上的 matcher 行为

## B 期待办
- Payload Localization + migration（staging `pnpm payload migrate:create` + `migrate`）
- sitemap hreflang / 多语言 URL 枚举
- Header/Footer 导航换 `createNavigation` 的 `Link`
- N8n 自动翻译管线（Payload REST API `?locale=all` 读 → 翻译 → PATCH 回写）

---

## B 期进度（2026-09-02 · 本次会话完成，**尚未 push**）

## Scope
- Payload Localization 落地：`localization`（fallback:true）+ 字段级 `localized: true`
  （pages/posts title、hero.richText、blocks richText、link.label；forms/search/header/footer 由插件/共享字段覆盖）
- 迁移 `src/migrations/20260902_081821_localization.ts` 生成并**已对 Neon 库执行**
- 前台按 locale 取内容：`src/i18n/getPayloadLocale.ts` + pages/posts/globals/search/archive 查询带 locale
- Sitemap 多语言：pages/posts-sitemap route handler 6 locale 枚举 + hreflang 互备 + x-default
- 导航 i18n：HeaderNav/Footer/CMSLink/Card/Pagination/Search/not-found 换 `createNavigation` Link/useRouter
- B 期 4 = Payload API 文档：`documention/plans/ongoing/n8n-payload-translation-api.md`

## Files
- `src/payload.config.ts`（localization）、`src/i18n/{localization,routing,request,getPayloadLocale}.ts`
- 字段：`src/collections/{Pages,Posts}/index.ts`、`src/fields/link.ts`、`src/heros/config.ts`、
  `src/blocks/{CallToAction,Content,Banner,ArchiveBlock,Form}/config.ts`
- 查询：`src/utilities/getGlobals.ts`、`[slug]`、`posts/[slug]`、`posts/page*`、`search`、`ArchiveBlock`
- 导航：`src/components/Link`、`Header/{Component.client,Nav}`、`Footer/Component`、`Card`、`Pagination`、`search/Component`、`not-found`
- Sitemap：`src/app/(frontend)/(sitemaps)/localized.ts` + 两个 route.ts + `next-sitemap.config.cjs`
- 迁移：`src/migrations/20260902_081821_localization.{ts,json}` + `index.ts`
- 文档：`documention/harness/MEMORY.md`、`documention/plans/ongoing/{i18n-next-intl-20260902-1445.md, n8n-payload-translation-api.md}`

## Validation
- `pnpm build` 全绿，6 locale 页面全部 SSG；sitemap postbuild 成功
- `pnpm exec tsc --noEmit` 0 错误；`pnpm lint` 0 错误（17 条既有 unused-args 警告）；guardrails PASS
- 冒烟（next start :3000）：`/`200、`/en`307(→根)、`/pt|es|fr|ru`200、`/ar`200 `dir=rtl`、`/pt/search`200、`/admin/login`200
- `pages-sitemap.xml` 实测含 6 locale + x-default hreflang 互备

## Deployment Notes
- 生产部署前：当前 `vercel.json` build 只有 `pnpm build`（不含 `payload migrate`）。
  本地已对 Neon 迁移；首次部署生产库前需跑一次 `pnpm payload migrate`，或给
  `db` adapter 配 `prodMigrations`（影响 serverless 冷启动，慎用）。
- 无新增 env；`?locale=all` 读、`?locale=xx` 写为翻译管线预留。
- 未 push：本 B 期改动待 review 后提交（涉及 DB migration 落库）。
