# i18n B 期（主体完成）进度存档

## Context
- 目标：为 ballisticbuddy.com（Payload CMS + Next 16）做多语言。A 期（next-intl 路由/UI 字典基础设施）已完成并推送；
  B 期（正文内容本地化 + 自动翻译管线 + SEO 多语言）主体已在 2026-09-02 本次会话完成。
- 语言集合（来自 `documention/plans/seo-foundation/`）：en(默认) / pt / ar(RTL) / es / fr / ru，
  单一事实来源 `src/i18n/localization.ts`（已 `as const` 出字面量联合）。

## Done（B 期）
- **Payload Localization**：`src/payload.config.ts` 挂 `localization`（`fallback: true`），
  字段级 `localized: true`（pages/posts 的 title、hero.richText、blocks richText、link.label、
  forms/search/header/footer 由插件/共享字段自动覆盖）。
  迁移文件 `src/migrations/20260902_081821_localization.ts` 已生成**并已对 Neon 库执行**
  （`payload migrate` 成功，`*_locales` 表全部就位；基础表全空，无数据丢失风险）。
- **前台按 locale 取内容**：新增 `src/i18n/getPayloadLocale.ts`（收窄 `PayloadLocale`），
  pages/[slug]、posts/[slug]、posts 列表/分页、search、ArchiveBlock、Header/Footer(globals)
  全部按当前 locale 查询；globals 缓存键含 locale。
- **Sitemap 多语言**：`src/app/(frontend)/(sitemaps)/localized.ts` 共享 helper；
  pages/posts-sitemap.xml route handler 按 6 locale 枚举 URL + 全量 hreflang 互备 + x-default；
  `next-sitemap.config.cjs` 补 root `alternateRefs`。
- **导航 i18n**：HeaderNav/Footer 徽标/CMSLink/Card/Pagination/Search/not-found 换
  `createNavigation` 的 `Link`/`useRouter`（`@/i18n/routing`），内链自动带 locale 前缀。
- **B 期 4 交付（按用户要求=只写 Payload API 文档）**：
  `documention/plans/ongoing/n8n-payload-translation-api.md` —— 认证、`?locale=all` 读、
  字段清单、按 locale PATCH 回写、lexical richText 只翻文本节点规则、fallback/草稿语义、n8n 落地草图。

## Validation
- `pnpm build` 全绿；`[locale]/posts` 等 6 locale 全部 SSG；sitemap postbuild 成功。
- `pnpm exec tsc --noEmit` 0 错误；`pnpm lint` 0 错误（17 条既有 unused-args 警告）；guardrails PASS。
- 冒烟（next start :3000）：`/` 200、`/en`→307(重定向根，as-needed 预期)、`/pt`/`/es`/`/fr`/`/ru` 200、
  `/ar` 200 且 `dir=rtl`、`/pt/search` 200、`/admin/login` 200。
- `pages-sitemap.xml` 实测输出：每个 locale URL 带 en/pt/ar/es/fr/ru + x-default 互备。

## Pending（剩余，非阻塞）
- **N8n 自动翻译管线落地**：按 `n8n-payload-translation-api.md` 建 workflow（尚未建）。
- **内容翻译执行**：现有 pages/posts 均为空；写英文后跑翻译补齐 5 个 locale。
- **UI 字典 messages/{en,pt,ar,es,fr,ru}.json 接线**：A 期只建了基础设施，`useTranslations`
  尚未在页面使用（B 期 4 文档已说明「先 UI 字典 JSON，再 CMS 正文」）。
- **search 插件索引**：post 翻译后需重跑 beforeSync 刷新 `search` 集合的多语言索引。

## Blockers
- 无海外认证叙事、敏感品类合规（来自 SEO intake）——影响多语言文案，非技术阻塞。
- 生产部署前需确认 Vercel 上 `payload migrate` 的执行时机（当前 `vercel.json` build 只有
  `pnpm build`，不含 migrate；本地已迁移，但生产库首次部署前要跑一次迁移或加 `prodMigrations`）。

## Resume Steps
1. `git checkout feat/i18n-next-intl-a`（如已提交则基于当前分支）。
2. 跑一次 `pnpm payload migrate` 确认本地库最新（迁移文件已在 repo）。
3. N8n：按 `documention/plans/ongoing/n8n-payload-translation-api.md` 建翻译 workflow。
4. 写英文正文 → 翻译补齐；前台验证 `/pt/{slug}`、`/ar/{slug}` 等。
5. 验证：`pnpm build` + `pnpm lint` + `pnpm exec tsc --noEmit` + `node scripts/check-guardrails.mjs`。
