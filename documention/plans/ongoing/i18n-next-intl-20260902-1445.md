# i18n B 期（待续）进度存档

## Context
- 目标：为 ballisticbuddy.com（Payload CMS + Next 16）做多语言。A 期（next-intl 路由/UI 字典基础设施）已完成并推送；B 期（正文内容本地化 + 自动翻译管线 + SEO 多语言）未开始，本次会话到此打住。
- 语言集合（来自 `documention/plans/seo-foundation/`）：en(默认) / pt / ar(RTL) / es / fr / ru，单一事实来源 `src/i18n/localization.ts`。

## Done
- next-intl 4.14.2 基础设施：`src/i18n/{localization,routing,request}.ts` + `src/proxy.ts` + `next.config.ts` 挂 `createNextIntlPlugin`
- 前台路由下移 `(frontend)/[locale]/`，`localePrefix: 'as-needed'`（英文在根 `/`）
- `messages/{en,pt,ar,es,fr,ru}.json` + `src/global.d.ts` 类型增强
- 分支 `feat/i18n-next-intl-a`（commit `0f27edb`）已 push origin

## Pending
- **Payload Localization**：`payload.config.ts` 挂 `localization`（字段级 `localized: true`），staging 跑 migration
- **Sitemap 多语言**：`next-sitemap` `alternateRefs` hreflang + `pages/posts-sitemap.xml` 按 locale 枚举 URL
- **导航 i18n**：Header/Footer 换 `createNavigation` 的 `Link`
- **N8n 自动翻译管线**：Payload REST `?locale=all` 读 → DeepL/AI 翻译 → PATCH 回写各 locale（rich text 需处理 lexical editorState，勿整段翻 JSON）

## Blockers
- 无海外认证叙事、敏感品类合规（来自 SEO intake）——影响多语言文案，非技术阻塞
- Payload `localization` 一开立即需要 `*_locales` 表 → 生产必须带 migration（不能在 A 期无迁移直接上）
- `next/root-params` 与 next-intl middleware 不兼容（已用 `requestLocale` 绕过，见 MEMORY.md i18n 节）

## Resume Steps
1. `git checkout feat/i18n-next-intl-a`；`pnpm dev` 重启
2. B 期 1：`src/payload.config.ts` 加 `localization` → `pnpm payload migrate:create` + `migrate`（先 staging）
3. B 期 2：next-sitemap `alternateRefs` + sitemap route handler 按 locale 枚举
4. B 期 3：Header/Footer 换 `createNavigation` Link
5. B 期 4：N8n 翻译（先 UI 字典 JSON，再 CMS 正文）
6. 验证：`pnpm build` + `pnpm lint` + `node scripts/check-guardrails.mjs`
